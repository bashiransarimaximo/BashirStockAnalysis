# Generated data -- regenerated by the pipeline on every run, not versioned.
# The GitHub Actions workflow commits fresh copies of these after each run
# (see .github/workflows/), so history for them lives in commit log, not here.
market_data/raw/
market_data/ta/
market_data/metadata/

# Local tooling / editor state, not part of the deployable pipeline.
.claude/
__pycache__/
*.pyc
.venv/
venv/
